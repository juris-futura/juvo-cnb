---
name: CI

on: push

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - actions/checkout@v4
      - actions/setup-go@v5
        with:
          go-version-file: buildpacks/go.mod
      - run: go version
